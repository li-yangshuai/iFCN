add_library(autopr INTERFACE)
target_include_directories(autopr INTERFACE ${PROJECT_SOURCE_DIR}/include)

add_library(simon INTERFACE)
target_include_directories(simon INTERFACE ${PROJECT_SOURCE_DIR}/include)
